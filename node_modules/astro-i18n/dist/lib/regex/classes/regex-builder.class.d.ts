import Regex from "../../regex/classes/regex.class";
declare class RegexBuilder {
    #private;
    constructor({ source, flags }: RegExp);
    static fromRegex(regex: Regex): RegexBuilder;
    appendPattern(pattern: string): this;
    matchTrimifiable(): this;
    assertStarting(): this;
    assertEnding(): this;
    addGroup(): this;
    addGlobalFlag(): this;
    build(): Regex;
}
export default RegexBuilder;
