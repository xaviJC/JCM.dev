/**
 * Similar to `Array.prototype.map` but replaces items on the same array instead
 * of returning a new one.
 */
export declare function replace<T>(callback: (value: T, index: number, array: T[]) => unknown, array: T[]): T[];
