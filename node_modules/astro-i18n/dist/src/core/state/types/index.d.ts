import type { SerializedConfig } from "../../../core/config/types";
import type { SegmentTranslations } from "../../../core/routing/types";
import type { SerializedFormatters, SerializedTranslationMap } from "../../../core/translation/types";
export type SerializedAstroI18n = {
    locale: string;
    route: string;
    config: SerializedConfig;
    translations: SerializedTranslationMap;
    segments: SegmentTranslations;
    formatters: SerializedFormatters;
};
