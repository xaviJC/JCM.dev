import type { Formatters, SerializedFormatters } from "../../../core/translation/types";
/**
 * This class stores formatters to be able to serialize only the custom
 * formatters.
 */
declare class FormatterBank {
    #private;
    constructor(customFormatters?: Formatters);
    get default(): Formatters;
    get custom(): Formatters;
    addFormaters(formatters: Formatters): this;
    clear(): void;
    toClientSideObject(): SerializedFormatters;
    toObject(): Formatters;
    toString(): string;
}
export default FormatterBank;
