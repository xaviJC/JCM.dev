import { ValueType } from "../../../../core/translation/enums/value-type.enum";
import type { FormatterMatch } from "../../../../core/translation/types";
/**
 * Matches every part of an interpolation and returns them separately.
 * @param interpolation for example `"'value'(alias)>formatter(arg)"`.
 */
export declare function matchInterpolation(interpolation: string): {
    raw: string;
    value: string;
    type: ValueType;
    alias: string | null;
    formatters: FormatterMatch[];
};
export declare function matchInterpolationVariables(interpolation: string): string[];
