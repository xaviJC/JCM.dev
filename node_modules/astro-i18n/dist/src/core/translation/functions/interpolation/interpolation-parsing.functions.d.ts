import { ValueType } from "../../../../core/translation/enums/value-type.enum";
import type { FormatterMatch, Formatters, TranslationProperties } from "../../../../core/translation/types";
/**
 * Creates the interpolation value for the given `value`.
 * @param value for example: `"{ prop: interpolationValue }"`.
 */
export declare function parseInterpolationValue(value: string, alias: string | null, type: ValueType, formatters: FormatterMatch[], properties: TranslationProperties, availableFormatters: Formatters): unknown;
