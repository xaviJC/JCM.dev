import { ValueType } from "../../../../core/translation/enums/value-type.enum";
/**
 * Matches a variant's property value.
 * @param value for example `"a string value"`
 */
export declare function matchVariantValue(value: string): {
    value: string;
    type: ValueType;
    end: number;
};
