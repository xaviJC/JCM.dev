import type { ParsedArgv } from "../../../../lib/argv/types";
declare const cmd: {
    readonly name: "extract";
    readonly options: readonly ["root"];
};
export declare function extract({ command, options }: ParsedArgv): Promise<void>;
export default cmd;
