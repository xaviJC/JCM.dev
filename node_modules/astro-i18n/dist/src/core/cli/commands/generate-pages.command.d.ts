import type { ParsedArgv } from "../../../../lib/argv/types";
declare const cmd: {
    readonly name: "generate:pages";
    readonly options: readonly ["purge", "root"];
};
export declare function generatePages({ command, options }: ParsedArgv): Promise<void>;
export default cmd;
