import type { ParsedArgv } from "../../../../lib/argv/types";
declare const cmd: {
    readonly name: "install";
    readonly options: readonly ["root", "serverless"];
};
export declare function install({ command, options }: ParsedArgv): Promise<void>;
export default cmd;
