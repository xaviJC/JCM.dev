import type { ParsedArgv } from "../../../../lib/argv/types";
declare const cmd: {
    readonly name: "generate:types";
    readonly options: readonly ["root"];
};
export declare function generateTypes({ command, options }: ParsedArgv): Promise<void>;
export default cmd;
